class Lot{ 
    constructor(name, spacesTaken, fullPer, filename, url){ 
        this.name = name; 
        this.spacesTaken = spacesTaken; 
        this. fullPer = fullPer; 
        this.filename = filename; 
        this.url = url; 
    }
}

module.exports = Lot; 